version.config
ez2on_dumper.js
decrypted_bundles/
il2cpp_out/
true_key_1024.bin
extracted_assets/
EZ2ON REBOOT R
EZ2ON REBOOT R/
__pycache__/
*.pyc
tools/build/


# added 2026-09-18 (repo tidy-up)
.venv/
data/_namemap.json
mitm_live/
mitm_parsed/flow_dumps

# third-party reference material (EZ2DJ/EZ2AC tooling, github.com/freem/ez2stuff)
reference/

# --- capture/dump artifacts (game memory + network traffic) — never publish ---
# these are raw dumps of live game state and captured API/CDN traffic; the
# pre-scrub history shipped them, so keep them out of the repo entirely.
data/
logs/
mitm_live/
mitm_parsed/
extracted_charts/
il2cpp_code/
tools/**/*_out.json

# rendered audio (derived from extracted_assets + charts)
rendered_songs/

# derived from captured API traffic (see the capture rules above); regenerate with
# chart_labels.py
chart_labels.json

# harvest_key.py output/backup
EZ2ON REBOOT R/decrypted_header_ram.bin
decrypted_header_ram.bin
true_key_1024.bin.bak

# game metadata cache (regenerate with harvest_metadata.py)
music_names.json

# visualiser output (derived from charts + BGAs)
visualizations/

# screen-state classifier artifacts (game screenshots: UI + BGA preview art)
server/shots/
server/anchors/

# --- private server (server/) runtime artifacts — never publish ---
# session_key.json holds the live API session key; data/ holds decrypted
# templates from your own captures (personal profile, play history, the music
# DB) and the chart-cipher blob cache. Regenerate with server/_build_data.py.
server/session_key.json
server/pserver.log
server/mitmdump.out
server/app.out
server/relay.out
server/login_probe.log
server/harvest_mem.log
server/harvester.out
server/data/
server/errwatch.log
# raw-channel stub certificate (signed by the local mitmproxy CA) + its log
server/data/stub443-cert.pem
server/data/stub443-key.pem
server/data/stub443.csr
server/data/stub443-cert.srl
server/stub443.log
server/cmd*.json
# audit-channel captures (raw game traffic; may contain session material)
server/stub9902.log
server/stub9902_last.bin
server/stub9902_client.bin
server/last_probe.json
server/hunt_result.json

.env
